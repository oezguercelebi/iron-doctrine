# Surprise — slice-1-playable closed

What we thought: a catalog-only repo; first playable after a stack page; two review blockers would wait.

What was true: Godot 4.7.2 .NET was already the right stable. The match shipped, then a user-authorized feel wave closed Guard-follow, local unload, persistent rally, sell cursor, ghost=place.

## Provisionals accepted at close

- Engine: 4.7.2 until 4.8 stable.
- No optional patriot link.
- Original Blender glTF, working labels, placeholder numbers in `data/slice1.placeholders.json`.
- `CanPlace` blocks on visible units so a green ghost places (catalog said units do not block ghosts; player-visible truth won).

## Spec defects

- Patch-cap stop fired on Guard + unload that a complete first-playable spec should have named. Feel wave had to reopen them.
- Catalog `MECHANICS` still says ghosts ignore mobile units; the sim now does not.

## Follow-ons (not this case)

- LAN / lockstep P2P (roadmap 4).
- Numbers pass, product names, 4.8 jump.
- Forge, Veil, specialists, 4/6/8 maps.
- Align catalog ghost text with `CanPlace`, or revert occupancy.

## Verdicts

Lane R and case R on the feel range `f923e3f..b7a5417` approved. Frozen `tests/SeamConformance.cs` unchanged. No backend. User close 2026-09-14. Push at-close.
